name: Test Client

on:
  push:
    paths:
    - 'client/**'

jobs:
  test-client:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run tests
        run: echo "Client Test"