name: E2E Test

on:
  workflow_run:
    workflows: [Test Client, Test Server]
    types: [completed]

jobs:
  test-client:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run tests
        run: echo "Client Test"